const Orders = () => {
  return (
    <section className="panel">
      <h1>Order Management</h1>
      <p>Upload, validation, and sending flows will load here.</p>
    </section>
  );
};

export default Orders;
